# คู่มือการเริ่มต้นใช้งานระบบโรงเรือนอัตโนมัติ

🌐 **[English Version](README.md)**

## คุณสมบัติของระบบ

- **ควบคุมความชื้นอัตโนมัติ** - รดน้ำเมื่อดินแห้ง, เปิดพัดลมเมื่อชื้นเกินไป
- **จอ LCD Display** - แสดงสถานะแบบ Real-time บนจอ LCD 16x2 I2C
- **Watchdog Timer (WDT)** - ป้องกันระบบค้างจากสัญญาณรบกวน EMI
- **ป้องกัน EMI** - ใช้ Median filter และตรวจจับค่าผิดปกติ
- **Safe Mode** - ปิดอุปกรณ์อัตโนมัติเมื่อเกิดข้อผิดพลาดร้ายแรง

## สิ่งที่ต้องเตรียม

### อุปกรณ์ฮาร์ดแวร์

1. **Arduino Uno** (1 บอร์ด)
2. **Sensor วัดความชื้นในดิน** (Soil Moisture Sensor) (1 ตัว)
3. **Relay 4-Channel** แบบ Active-Low (1 โมดูล)
4. **ปั๊มน้ำ DC** พร้อมท่อน้ำ (1 ตัว)
5. **พัดลม DC** (1 ตัว)
6. **LCD Display 16x2 (I2C)** สำหรับแสดงผล (1 จอ)
7. **สายไฟจัมเปอร์** (หลายเส้น)
8. **สาย USB Type-B** สำหรับต่อ Arduino กับคอมพิวเตอร์
9. **แหล่งจ่ายไฟ** สำหรับปั๊มน้ำและพัดลม

### ซอฟต์แวร์ที่ต้องติดตั้ง

- **Arduino IDE 2.x** (โปรแกรมสำหรับเขียนและอัปโหลดโค้ด)
- **LiquidCrystal I2C Library** (ไลบรารีสำหรับควบคุม LCD)

---

## ขั้นตอนที่ 1: ต่อสายวงจร

### 1.1 ต่อ Sensor วัดความชื้นในดิน

| ขา Sensor | ต่อกับ Arduino |
| --------- | -------------- |
| VCC       | 5V             |
| GND       | GND            |
| AO        | A0             |
| DO        | ไม่ต้องต่อ     |

### 1.2 ต่อ Relay 4-Channel (12V)

| ขา Relay (Input) | ต่อกับ...         | รายละเอียด                              |
| ---------------- | ----------------- | --------------------------------------- |
| VCC              | Arduino 5V (+)    | ต่อขั้วบวกจาก Arduino 5V                |
| JD-VCC           | แบตเตอรี่ 12V (+) | **ห้ามต่อ +12V กับ Arduino โดยตรง**     |
| GND              | แบตเตอรี่ 12V (-) | **Arduino GND ต่อเข้า Sensor เท่านั้น** |
| IN1              | Arduino D2        | (สำรอง)                                 |
| IN2              | Arduino D3        | (สำรอง)                                 |
| IN3              | Arduino D4        | ควบคุมปั๊มน้ำ                           |
| IN4              | Arduino D5        | ควบคุมพัดลม                             |

### 1.3 ต่อ LCD Display 16x2 (I2C)

| ขา LCD | ต่อกับ Arduino | รายละเอียด      |
| ------ | -------------- | --------------- |
| VCC    | 5V             | ไฟเลี้ยง 5V     |
| GND    | GND            | กราวด์          |
| SDA    | A4             | สาย Data (I2C)  |
| SCL    | A5             | สาย Clock (I2C) |

> [!TIP]
> **หมายเหตุ**: ที่อยู่ I2C ของ LCD มักจะเป็น `0x27` หรือ `0x3F` ขึ้นอยู่กับรุ่น
> ถ้าไม่แน่ใจ สามารถใช้โปรแกรม I2C Scanner เพื่อตรวจสอบที่อยู่ได้

### 1.4 ต่อปั๊มน้ำและพัดลม

- **ปั๊มน้ำ**: ต่อเข้า Relay ช่อง 3 (IN3)
- **พัดลม**: ต่อเข้า Relay ช่อง 4 (IN4)

**การต่อสายไฟด้าน Output (ช่องเสียบสกรู):**

| ช่อง (Terminal) | ชื่อเต็ม        | การเชื่อมต่อ                                   |
| --------------- | --------------- | ---------------------------------------------- |
| **COM**         | Common          | ต่อกับ **ขั้วบวก (+) ของแหล่งจ่ายไฟ**          |
| **NO**          | Normally Open   | ต่อกับ **ขั้วบวก (+) ของอุปกรณ์ (ปั๊ม/พัดลม)** |
| **NC**          | Normally Closed | **ไม่ต้องต่อ** (ปล่อยว่าง)                     |

_ขั้วลบ (-) ของอุปกรณ์ ต่อเข้ากับขั้วลบ (-) ของแหล่งจ่ายไฟโดยตรง_

> [!WARNING]
> ระวังเรื่องขั้วไฟ และควรใช้แหล่งจ่ายไฟแยกสำหรับปั๊มน้ำและพัดลม

---

## ขั้นตอนที่ 2: ติดตั้ง Arduino IDE

### สำหรับ Windows

1. ไปที่เว็บไซต์ https://www.arduino.cc/en/software
2. คลิก **"Windows Win 10 and newer, 64 bits"**
3. คลิก **"Just Download"** (ไม่ต้องบริจาคก็ได้)
4. เปิดไฟล์ที่ดาวน์โหลดมา แล้วทำตามขั้นตอนการติดตั้ง
5. คลิก **"I Agree"** > **"Next"** > **"Install"**
6. รอจนติดตั้งเสร็จ แล้วคลิก **"Close"**

### สำหรับ macOS

1. ไปที่เว็บไซต์ https://www.arduino.cc/en/software
2. คลิก **"macOS"**
3. คลิก **"Just Download"**
4. เปิดไฟล์ .dmg ที่ดาวน์โหลดมา
5. ลาก Arduino IDE ไปใส่โฟลเดอร์ Applications

### สำหรับ Linux

1. ไปที่เว็บไซต์ https://www.arduino.cc/en/software
2. คลิก **"Linux AppImage 64 bits"**
3. เปิด Terminal แล้วพิมพ์:
   ```
   chmod +x arduino-ide_*_Linux_64bit.AppImage
   ./arduino-ide_*_Linux_64bit.AppImage
   ```

---

## ขั้นตอนที่ 3: ติดตั้ง LiquidCrystal I2C Library

### 3.1 เปิด Library Manager

1. เปิดโปรแกรม **Arduino IDE**
2. คลิก **Tools** > **Manage Libraries...** (หรือกด `Ctrl + Shift + I`)
3. หน้าต่าง Library Manager จะเปิดขึ้นมา

### 3.2 ค้นหาและติดตั้ง Library

1. ในช่องค้นหา พิมพ์ **"LiquidCrystal I2C"**
2. มองหา Library ชื่อ **"LiquidCrystal I2C"** โดย **Marco Schwartz**
3. คลิกที่ Library แล้วคลิกปุ่ม **"Install"**
4. รอจนติดตั้งเสร็จ (จะแสดงคำว่า "INSTALLED")
5. คลิก **"Close"** เพื่อปิดหน้าต่าง

### 3.3 ตรวจสอบการติดตั้ง

1. คลิก **File** > **Examples**
2. เลื่อนลงมาจะเห็น **LiquidCrystal I2C** ในรายการ
3. ถ้าเห็นแสดงว่าติดตั้งสำเร็จแล้ว

### 3.4 (ถ้าต้องการ) ตรวจสอบที่อยู่ I2C ของ LCD

1. ดาวน์โหลดโค้ด **I2C Scanner** จาก Arduino Examples
2. คลิก **File** > **Examples** > **Wire** > **i2c_scanner**
3. อัปโหลดโค้ดไปยัง Arduino (ต่อ LCD ไว้แล้ว)
4. เปิด **Serial Monitor** (`Ctrl + Shift + M`)
5. ดูที่อยู่ I2C ที่แสดงผล (เช่น `0x27` หรือ `0x3F`)
6. นำที่อยู่นี้ไปใช้ในโค้ด (ถ้าไม่ใช่ `0x27` ให้แก้ไขในไฟล์ `Config.h`)

```cpp
namespace LcdConfig {
  constexpr uint8_t I2C_ADDRESS = 0x27;  // เปลี่ยนเป็น 0x3F ถ้าต้องการ
  ...
}
```

---

## ขั้นตอนที่ 4: ตั้งค่าโปรเจกต์ใน Arduino IDE

### โครงสร้างไฟล์โปรเจกต์

โปรเจกต์นี้ใช้หลายไฟล์เพื่อจัดระเบียบโค้ด:

```
GreenhouseSystem/
├── GreenhouseSystem.ino    (ไฟล์หลัก - เปลี่ยนชื่อจาก main.cpp)
├── Config.h                (ค่าคงที่ต่างๆ)
├── Watchdog.h              (ส่วนประกาศ Watchdog Timer)
├── Watchdog.cpp            (ส่วนทำงาน Watchdog Timer)
├── Sensor.h                (ส่วนประกาศ Sensor)
├── Sensor.cpp              (ส่วนทำงาน Sensor)
├── Relay.h                 (ส่วนประกาศควบคุม Relay)
├── Relay.cpp               (ส่วนทำงานควบคุม Relay)
├── StateMachine.h          (ส่วนประกาศ State Machine)
├── StateMachine.cpp        (ส่วนทำงาน State Machine)
├── Display.h               (ส่วนประกาศแสดงผล)
└── Display.cpp             (ส่วนทำงานแสดงผล)
```

### 4.1 สร้างโฟลเดอร์โปรเจกต์

1. สร้างโฟลเดอร์ใหม่ชื่อ **"GreenhouseSystem"** บนคอมพิวเตอร์
2. ชื่อโฟลเดอร์ต้องตรงกับชื่อไฟล์ sketch หลัก

### 4.2 ดาวน์โหลดและคัดลอกไฟล์

1. ดาวน์โหลดไฟล์ทั้งหมดจาก repository นี้
2. คัดลอกไฟล์ต่อไปนี้ไปยังโฟลเดอร์ **"GreenhouseSystem"**:

**จากโฟลเดอร์ `src/`:**

- `main.cpp` → เปลี่ยนชื่อเป็น **`GreenhouseSystem.ino`**
- `Watchdog.cpp`
- `Sensor.cpp`
- `Relay.cpp`
- `StateMachine.cpp`
- `Display.cpp`

**จากโฟลเดอร์ `include/`:**

- `Config.h`
- `Watchdog.h`
- `Sensor.h`
- `Relay.h`
- `StateMachine.h`
- `Display.h`

### 4.3 เปิดโปรเจกต์ใน Arduino IDE

1. เปิดโปรแกรม **Arduino IDE**
2. คลิก **File** > **Open**
3. ไปที่โฟลเดอร์ **"GreenhouseSystem"**
4. เลือกไฟล์ **"GreenhouseSystem.ino"** แล้วคลิก **Open**
5. จะเห็นไฟล์ทั้งหมดปรากฏเป็นแท็บใน IDE

> [!IMPORTANT]
> **ไฟล์ทั้งหมดต้องอยู่ในโฟลเดอร์เดียวกับไฟล์ .ino**
> Arduino IDE จะรวมไฟล์ .h, .cpp และ .ino ทั้งหมดในโฟลเดอร์ sketch โดยอัตโนมัติ

### 4.4 ทางเลือก: ตั้งค่าแบบไฟล์เดียว (ง่ายกว่า)

ถ้าต้องการใช้แบบไฟล์เดียวให้ง่าย:

1. ดาวน์โหลด **เวอร์ชันไฟล์เดียว** จากหน้า [Releases](https://github.com/Scott-Nx/Automatic-Greenhouse-System/releases)
2. เปิด Arduino IDE
3. คลิก **File** > **New Sketch**
4. ลบโค้ดเดิมแล้ววางโค้ดที่ดาวน์โหลดมา
5. บันทึกเป็น **"GreenhouseSystem"**

---

## ขั้นตอนที่ 5: เชื่อมต่อ Arduino กับคอมพิวเตอร์

### 5.1 เสียบสาย USB

ใช้สาย USB Type-B เชื่อมต่อ Arduino Uno กับคอมพิวเตอร์

### 5.2 เลือกบอร์ด

1. คลิก **Tools** > **Board** > **Arduino AVR Boards** > **Arduino Uno**

### 5.3 เลือก Port

1. คลิก **Tools** > **Port**
2. เลือก Port ที่มีคำว่า **"Arduino Uno"** ต่อท้าย
   - Windows: จะเป็น `COM3`, `COM4`, หรือ `COM` อื่นๆ
   - macOS: จะเป็น `/dev/cu.usbmodem...`
   - Linux: จะเป็น `/dev/ttyACM0` หรือ `/dev/ttyUSB0`

> [!TIP]
> **ถ้าไม่เห็น Port**: ลองถอดสาย USB แล้วเสียบใหม่ หรือลองเปลี่ยนช่อง USB

---

## ขั้นตอนที่ 6: อัปโหลดโค้ดไปยัง Arduino

### 6.1 ตรวจสอบโค้ด (Verify)

1. คลิกปุ่ม **✓ (เครื่องหมายถูก)** ที่มุมซ้ายบน หรือกด `Ctrl + R`
2. รอจนแสดงข้อความ **"Done compiling."** ที่ด้านล่าง
3. ถ้ามีข้อผิดพลาด จะแสดงเป็นตัวอักษรสีส้ม/แดง

### 6.2 อัปโหลดโค้ด (Upload)

1. คลิกปุ่ม **→ (ลูกศร)** ที่อยู่ถัดจากปุ่ม Verify หรือกด `Ctrl + U`
2. รอจนแสดงข้อความ **"Done uploading."**
3. ไฟ LED บน Arduino จะกะพริบระหว่างอัปโหลด

---

## ขั้นตอนที่ 7: ดูผลการทำงาน

### 7.1 ดูผลจอ LCD Display

หลังจากอัปโหลดโค้ดเสร็จแล้ว LCD จะแสดงผล:

**หน้าจอเริ่มต้น (2 วินาทีแรก):**

```
🌱 Greenhouse
System v2.2 WDT
```

**หน้าจอแสดงสถานะ:**

```
🌱M: 65% OK
IDLE       123s
```

**คำอธิบายการแสดงผล:**

- **แถวที่ 1**:
  - ไอคอน (🌱=ปกติ, 💧=รดน้ำ, 🌀=ระบาย, ⚠=ผิดพลาด)
  - `M:XX%` = ค่าความชื้นเป็นเปอร์เซ็นต์
  - สถานะ (`OK`=ปกติ, `DRY`=แห้ง, `WET`=ชื้นเกิน)
- **แถวที่ 2**:
  - สถานะระบบ (`IDLE`, `WATERING`, `VENT`, `COOLDOWN`, `ERROR`)
  - เวลาที่ผ่านไปในสถานะปัจจุบัน (วินาที)

### 7.2 เปิด Serial Monitor

1. คลิก **Tools** > **Serial Monitor** หรือกด `Ctrl + Shift + M`
2. หน้าต่างใหม่จะเปิดขึ้นมา

### 7.3 ตั้งค่า Baud Rate

ที่มุมขวาล่างของ Serial Monitor ให้เลือก **"9600 baud"**

### 7.4 อ่านค่า

จะเห็นข้อความแสดงค่าความชื้นและสถานะของระบบ:

```
========================================
Automatic Greenhouse System v2.2
With Watchdog & EMI Protection
========================================

[BOOT] Reset reason: POWER-ON RESET
[RELAY] Initialized - All relays OFF
[SENSOR] Initialized
[LCD] Initialized (16x2 I2C)
[INIT] System initialization complete!
[STATE] Entering IDLE mode
[WDT] Watchdog Timer initialized (2s timeout)

-------------------------------------
Moisture: 450 (56%) | Status: NORMAL (OK)
System State: IDLE (5s)
Pump: OFF | Fan: OFF
-------------------------------------
```

---

## ขั้นตอนที่ 8: ปรับแต่งค่า (ถ้าต้องการ)

### ค่าที่สามารถปรับได้

เปิดไฟล์ **`Config.h`** แล้วหาส่วนต่อไปนี้:

**ค่าขีดจำกัดความชื้น:**

```cpp
namespace Config {
  constexpr int MOISTURE_DRY_THRESHOLD = 700;   // ค่าขีดจำกัดดินแห้ง
  constexpr int MOISTURE_WET_THRESHOLD = 300;   // ค่าขีดจำกัดดินชื้นเกินไป
  constexpr int HYSTERESIS = 50;                // ป้องกันการสั่น
  ...
}
```

**การตั้งค่าเวลา:**

```cpp
namespace Config {
  ...
  constexpr unsigned long PUMP_RUN_TIME       = 5000UL;   // 5 วินาที
  constexpr unsigned long FAN_RUN_TIME        = 10000UL;  // 10 วินาที
  constexpr unsigned long COOLDOWN_TIME       = 30000UL;  // 30 วินาที
  ...
}
```

**การตั้งค่า Watchdog:**

```cpp
namespace WatchdogConfig {
  constexpr uint8_t TIMEOUT = WDTO_2S;  // timeout 2 วินาที
  // ตัวเลือก: WDTO_1S, WDTO_2S, WDTO_4S, WDTO_8S
  ...
}
```

### วิธีปรับค่า

| ค่า                      | ความหมาย                  | คำแนะนำ                                  |
| ------------------------ | ------------------------- | ---------------------------------------- |
| `MOISTURE_DRY_THRESHOLD` | ค่าที่ถือว่าดินแห้ง       | ยิ่งเพิ่มค่า = ดินต้องแห้งมากถึงจะรดน้ำ  |
| `MOISTURE_WET_THRESHOLD` | ค่าที่ถือว่าดินชื้นเกินไป | ยิ่งลดค่า = ดินต้องชื้นมากถึงจะเปิดพัดลม |
| `PUMP_RUN_TIME`          | เวลาเปิดปั๊มน้ำ           | 5000 = 5 วินาที                          |
| `FAN_RUN_TIME`           | เวลาเปิดพัดลม             | 10000 = 10 วินาที                        |
| `COOLDOWN_TIME`          | เวลาพักหลังทำงาน          | 30000 = 30 วินาที                        |

### วิธีหาค่าที่เหมาะสม

1. เปิด Serial Monitor ดูค่าความชื้น
2. ทดสอบจุ่ม Sensor ในดินแห้ง จดค่าไว้
3. ทดสอบจุ่ม Sensor ในดินชื้น จดค่าไว้
4. ปรับค่า `THRESHOLD` ตามที่ต้องการ

---

## Watchdog Timer และการป้องกัน EMI

### Watchdog Timer คืออะไร?

Watchdog Timer (WDT) เป็นคุณสมบัติด้านความปลอดภัยที่จะรีเซ็ต Arduino อัตโนมัติถ้าโปรแกรมค้าง สิ่งนี้มีประโยชน์มากในสภาพแวดล้อมโรงเรือนที่สัญญาณรบกวนแม่เหล็กไฟฟ้า (EMI) จากปั๊มและพัดลมอาจทำให้ไมโครคอนโทรลเลอร์หยุดทำงาน

### การทำงาน

1. WDT ถูกตั้งค่า timeout 2 วินาที
2. โปรแกรมต้อง "pet" (รีเซ็ต) watchdog เป็นประจำ
3. ถ้าโปรแกรมค้างและไม่ได้ pet watchdog เป็นเวลา 2 วินาที Arduino จะรีเซ็ตอัตโนมัติ
4. หลังรีเซ็ต ระบบจะตรวจจับว่าเป็นการรีเซ็ตจาก watchdog และดำเนินการตามความเหมาะสม

### คุณสมบัติป้องกัน EMI

- **Median Filter**: อ่านค่า sensor 10 ครั้งและใช้ค่ากลางเพื่อกรอง noise spike
- **ตรวจจับ Spike**: ตรวจจับการเปลี่ยนแปลงค่า sensor ที่ผิดปกติมากซึ่งอาจเกิดจาก EMI
- **นับ Error ติดต่อกัน**: ถ้าเกิดข้อผิดพลาดติดต่อกันหลายครั้ง ระบบจะเข้าสู่ safe mode
- **Safe Mode**: ปิดอุปกรณ์ทั้งหมดอัตโนมัติเมื่อตรวจพบข้อผิดพลาดร้ายแรง

---

## การแก้ปัญหาเบื้องต้น

### ปัญหา: LCD ไม่แสดงผล

**อาการ**: จอ LCD ไม่มีอะไรแสดง หรือแสดงแต่กล่องสี่เหลี่ยม

**วิธีแก้**:

1. **ตรวจสอบการต่อสาย**:
   - VCC ต่อกับ 5V
   - GND ต่อกับ GND
   - SDA ต่อกับ A4
   - SCL ต่อกับ A5

2. **ตรวจสอบ Backlight**:
   - หมุนไปมาที่ตัวปรับความสว่าง (potentiometer สีน้ำเงินด้านหลัง LCD)
   - ถ้าเห็นแสงสว่างแต่ไม่มีตัวอักษร ให้หมุนไปอีกทาง

3. **ตรวจสอบที่อยู่ I2C**:
   - ใช้โค้ด I2C Scanner ตรวจสอบที่อยู่ (ดูขั้นตอนที่ 3.4)
   - ถ้าที่อยู่ไม่ใช่ `0x27` ให้แก้ไขใน `Config.h`

4. **ตรวจสอบ Library**:
   - ตรวจสอบว่าติดตั้ง LiquidCrystal I2C Library แล้ว
   - ลองถอดแล้วติดตั้งใหม่

### ปัญหา: Compile Error - "Multiple definition"

**อาการ**: ข้อความ error เกี่ยวกับการประกาศซ้ำของฟังก์ชันหรือตัวแปร

**วิธีแก้**:

1. ตรวจสอบว่ามี `SystemData systemData;` **เพียงที่เดียว** (ใน `StateMachine.cpp`)
2. ตรวจสอบว่าไฟล์ header ใช้ `#ifndef` / `#define` / `#endif` guards
3. ตรวจสอบว่าไฟล์ `.h` มีแต่ `extern` declarations ไม่ใช่ definitions

### ปัญหา: Compile Error - "File not found"

**อาการ**: ข้อความ error เช่น `'Config.h' file not found`

**วิธีแก้**:

1. ตรวจสอบว่าไฟล์ `.h` และ `.cpp` ทั้งหมดอยู่ใน**โฟลเดอร์เดียวกับ**ไฟล์ `.ino`
2. ตรวจสอบว่าชื่อโฟลเดอร์ตรงกับชื่อไฟล์ `.ino`
3. ปิดแล้วเปิด Arduino IDE ใหม่หลังจากเพิ่มไฟล์

### ปัญหา: LCD แสดงตัวอักษรแปลกๆ (Garbled Text)

**อาการ**: LCD แสดงตัวอักษรที่อ่านไม่ออก

**วิธีแก้**:

1. หมุนตัวปรับความสว่าง (Contrast Potentiometer) ด้านหลัง LCD
2. ตรวจสอบสาย SDA และ SCL ว่าต่อถูกต้อง
3. ตรวจสอบว่าไม่มีสายอื่นรบกวนสัญญาณ I2C

### ปัญหา: ระบบรีเซ็ตตลอด

**อาการ**: Serial Monitor แสดง "WATCHDOG RESET" ซ้ำๆ

**วิธีแก้**:

1. ตรวจสอบว่าไม่มี infinite loop ในโค้ดที่แก้ไข
2. เพิ่ม watchdog timeout ใน `Config.h`:
   ```cpp
   constexpr uint8_t TIMEOUT = WDTO_4S;  // เพิ่มเป็น 4 วินาที
   ```
3. ตรวจสอบว่า `watchdogReset()` ถูกเรียกเป็นประจำใน main loop

### ปัญหา: อัปโหลดไม่ได้

**อาการ**: แสดงข้อความ "avrdude: stk500_recv(): programmer is not responding"

**วิธีแก้**:

1. ตรวจสอบว่าเลือก Board เป็น "Arduino Uno"
2. ตรวจสอบว่าเลือก Port ถูกต้อง
3. ถอดสาย USB แล้วเสียบใหม่
4. ลองเปลี่ยนสาย USB

### ปัญหา: ค่าความชื้นไม่เปลี่ยน

**อาการ**: ค่าแสดงเป็น 0 หรือ 1023 ตลอด

**วิธีแก้**:

1. ตรวจสอบการต่อสายของ Sensor
2. ตรวจสอบว่า VCC ต่อกับ 5V, GND ต่อกับ GND
3. ตรวจสอบว่าสาย AO ต่อกับ A0

### ปัญหา: Relay ไม่ทำงาน

**อาการ**: ไม่ได้ยินเสียงคลิกจาก Relay

**วิธีแก้**:

1. ตรวจสอบการต่อสาย VCC และ GND ของ Relay
2. ตรวจสอบว่าสาย IN1-IN4 ต่อถูกขา
3. ตรวจสอบว่า Relay ได้รับไฟเพียงพอ

### ปัญหา: ปั๊มน้ำ/พัดลม ไม่ทำงาน

**อาการ**: Relay คลิก แต่ปั๊มน้ำ/พัดลมไม่หมุน

**วิธีแก้**:

1. ตรวจสอบการต่อสายปั๊มน้ำ/พัดลมกับ Relay
2. ตรวจสอบแหล่งจ่ายไฟของปั๊มน้ำ/พัดลม
3. ลองทดสอบปั๊มน้ำ/พัดลมโดยตรงกับแหล่งจ่ายไฟ

---

## ประวัติเวอร์ชัน

- **v2.2** - เพิ่ม Watchdog Timer, ป้องกัน EMI, โครงสร้างหลายไฟล์
- **v2.1** - เพิ่มการรองรับจอ LCD
- **v2.0** - ปรับโครงสร้าง State Machine
- **v1.0** - เวอร์ชันแรก

---

## สัญญาอนุญาต

โปรเจกต์นี้ใช้สัญญาอนุญาต MIT License - ดูรายละเอียดในไฟล์ [LICENSE](LICENSE)
